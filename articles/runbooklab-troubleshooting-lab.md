---
title: "AWS・CCNA/CCNP・LPICのトラブルシューティングを疑似体験できる演習サイトを作った話【RunbookLab】"
emoji: "🛠️"
type: "tech"
topics: ["dotnet", "blazor", "aws", "ccna", "lpic"]
published: true
published_at: 2026-08-11 09:00
---

## はじめに

**RunbookLab**（https://runbook-lab.sorapenguin.dev）は、AWS・Cisco（CCNA・CCNP ENCOR/ENARSI）・LPICの資格試験対策を、ログやコマンド出力から原因を特定するトラブルシューティング形式で学べる無料の演習サービスです。

主な特徴：

- 実際のCLI出力・AWS CLIレスポンスをもとに「調査→診断→対処→復旧確認」を体験
- 正答はサーバー側のみが保持し、クライアントには一切渡さない設計
- AWS CLF・CCNA・CCNP ENARSI・CCNP ENCOR・LPIC（101/102/201/202/300/303/305/306）を横断し、450シナリオを収録
- .NET 10 + Blazor WASM + PostgreSQLで構築

この記事では「なぜサーバー権威型の設計にしたか」と「シナリオをどう量産・検証しているか」について書きます。

![RunbookLabのトップページ。LPIC/AWS/CCNA/CCNPのカテゴリカードが並ぶ](/images/runbooklab/home.png)

---

## なぜ作ったか

CCNA/CCNPやLPICの資格試験には「コマンド操作を暗記していれば解ける」四択問題が多く、実務で求められる「ログを読んで原因を切り分ける力」を測る演習が世の中に少ないと感じていました。

以前作った[RouteLab](https://routelab.sorapenguin.dev)（CLIコマンドをスマホで練習できるシミュレーター）を運用する中で、「自由な順序でコマンドを入力できる」設計が保守の負債になっていくのを経験しました。RunbookLabはこの反省を踏まえ、「状況を観察し、仮説を立てて調査し、原因を診断し、対処して、復旧を確認する」という決まった手順（Observe→Investigate→Diagnose→Remediate→Verify→Review）を軸にした、別設計のC#アプリとして作り直しています。

---

## サーバー権威型のスコアリング設計

RunbookLabの技術的な核は「正答をクライアントに一切公開しない」設計です。

- 出題データはシナリオごとに `ScenarioPrivate` として分離し、Blazor WASM（ブラウザ側）に配信されるレスポンスには含まれません
- 採点はすべてサーバー側（ASP.NET Core Minimal API）で実行し、診断・修復・検証をそれぞれ最大30点、調査効率は一律10点の固定点という配点で評価します（間違えるたびに減点、合計100点満点）
- 状態管理はPostgreSQL（EF Core）で行い、`SELECT FOR UPDATE` と二重チェックによる冪等性制御・リクエスト指紋（SHA-256ハッシュ）による誤操作検知を実装しています
- Fault injectionテスト（DB書き込み直後やコミット前に例外を注入するテスト）を含む100件超のxUnitテストで、この冪等性設計を担保しています

ブラウザ側だけで完結するRouteLabと違い、「答えを覗き見できない」「同時多重送信で状態が壊れない」という信頼性を優先した設計です。

---

## シナリオの量産・検証パイプライン

コンテンツはCodex（コーディングエージェント）に生成させ、Claude Codeが独立に検証してからコミットするパイプラインで作っています。

```
[1] 試験の公式ブループリント・出題観点をもとにトピック表を作成
      ↓
[2] Codexにシナリオ（症状・調査証拠・診断・対処・検証）をJSON形式で生成させる
      ↓
[3] 構造バリデータ（C#側）でスキーマ・カテゴリ整合をチェック
      ↓
[4] 実際のCisco/AWS公式コマンドリファレンスと照合し、捏造コマンドを検出
      ↓
[5] テンプレート重複検出（既存シナリオとの類似度チェック）
      ↓
[6] ChatGPT Thinkingへレビュー依頼（保護フィールドはSHA256比較で改変不可）
      ↓
[7] 人間が最終確認してから本番投入
```

この過程で、存在しないCiscoコマンド（例: 引数の組み合わせが公式構文上あり得ないもの）をAIが生成してしまうケースを実際に複数回検出しました。機械的なスキーマチェックだけでは気づけないため、公式ドキュメントとの個別照合を必須工程にしています。

現在、AWS CLF・CCNA・CCNP ENARSI（120問）・CCNP ENCOR・LPIC（101/102/201/202/300/303/305/306）を合わせて450シナリオを実装しています。

![カテゴリを選ぶとシナリオ一覧が表示される（LPIC-1 101の例）](/images/runbooklab/category.png)

シナリオを選ぶと、「調査→診断→対処→復旧確認→結果・解説」の5タブで構成された演習画面に進みます。

![シナリオの調査フェーズ。症状と調査操作の選択肢が表示される](/images/runbooklab/attempt.png)

---

## 音声学習モード

移動中や家事をしながらでも学べるよう、シナリオの症状・診断・対処・検証をナレーション音声で聞ける「音声学習モード」も用意しています（244シナリオ分を生成済み）。

![音声学習モードの画面。上部に再生バー、下に症状・着目点・原因・対処などがブロックごとに並ぶ](/images/runbooklab/audio.png)

- **音声合成**: ローカルで動かしているTTSエンジン（AivisSpeech）でシナリオごとに事前生成し、`<audio>`要素で再生するだけのシンプルな構成。ブラウザのWeb Speech APIではなく事前生成にしたのは、読み上げ品質と再現性（毎回同じ音声になる）を優先したためです
- **ハイライト同期**: 再生中の文にあわせて本文をハイライトし、自動スクロールで追従します。当初は画像を`position: sticky`で固定していましたが、スクロール量によってハイライト対象の文が画像の裏に隠れる問題が出たため、「ヘッダー・画像・再生バーをピン留めし、本文だけを独立スクロールペインにする」構成に作り直しました
- **間の取り方**: 文と文の間に0.4秒、症状→着目点→原因のようなブロックの切り替わりには2秒の無音区間を挟んでいます。棒読みで詰め込むと聞き取りづらいので、区切りの長さを意図的に変えて呼吸を作っています

シナリオの挿絵は、Codex CLIの画像生成ツール（`image_gen`）に生成させています。障害の状況が一目でわかるスケッチ風の一枚絵をプロンプトで指示し、生成から保存まで自律的に実行させる運用です。

---

## RouteLabとの違い

同じ「CLI/コマンド学習」を扱う姉妹サービスですが、設計思想は対照的です。

| 項目 | RouteLab | RunbookLab |
|---|---|---|
| 技術 | React + TypeScript + Express | C# .NET 10 + Blazor WASM |
| 採点 | クライアント側中心 | サーバー側のみ |
| 正答の扱い | ブラウザのJSONに含まれる | サーバーメモリのみ、クライアントに非公開 |
| 状態管理 | localStorage | PostgreSQL（冪等性・並行制御あり） |
| コマンド入力 | 自由入力（実際にタイプする） | 状況を選んで調査を進めるガイド型 |

「実機のコマンド入力に近い体験」はRouteLab、「実務のトラブルシュート手順そのものを疑似体験する」のがRunbookLab、という役割分担にしています。

---

## 無料で使えます

**https://runbook-lab.sorapenguin.dev**

登録不要・無料で全シナリオに挑戦できます。今後もシナリオを追加していく予定です。

---

## まとめ

- RouteLabの設計負債を踏まえ、C#・サーバー権威型で作り直した資格試験対策のトラブルシューティング演習サービス
- 正答はクライアントに一切渡さず、DBでの冪等性制御・並行制御まで実装
- AI（Codex）によるシナリオ生成→機械検証→公式ドキュメント照合→ChatGPTレビュー→人間確認、という多段階パイプラインで450シナリオを品質担保
- AWS・CCNA・CCNP ENARSI/ENCOR・LPICを横断カバー
- ながら学習用の音声モードも実装（244シナリオ分、日本語の文末処理バグと格闘した話つき）
- 無料で誰でも使えます

---

*C# .NET 10 / ASP.NET Core Minimal API / Blazor WASM / EF Core / PostgreSQL / xUnit / Docker / Traefik で構築・VPS本番稼働中*
